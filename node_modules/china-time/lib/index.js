
const moment = require('moment-timezone');

function chinaTime (format) {
  return format
    ? moment().tz('Asia/Shanghai').format(format)
    : new Date(moment().tz('Asia/Shanghai').format('YYYY-MM-DD HH:mm'));
}

module.exports = chinaTime;
